{
  "NLog": {
    "targets": {
      "Debugger": {
        "type": "Debugger",
        "layout": "${simplePipeSeparatorLayout}"
      }
    },
    "rules": [
      {
        "logger": "*",
        "minLevel": "Info",
        "writeTo": "Debugger"
      }
    ]
  }
}